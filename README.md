# mini-sql-engine
A small SQL engine (hand-written lexer/parser/planner/executor) that runs SELECT queries — including joins, GROUP BY/HAVING, ORDER BY, aggregates — over in-memory CSV tables. Python stdlib only.
